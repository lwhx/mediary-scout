{
  "name": "media-track",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "sh -c 'vitest run --passWithNoTests'",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
